topic: ~main()
# Defining extra concepts out of words or group of words
concept:(execute) [execute executor]

# Replying to speech
proposal: %execute_proposal Say execute or executor to test the functionality.
u:(~execute) I lunch the execute ^execute(myExecutor, param1, param2)  and now i continue